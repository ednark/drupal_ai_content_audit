site-dashboard:
  version: VERSION
  css:
    component:
      css/ai-site-dashboard.css: {}
  js:
    js/ai-site-dashboard.js: {}
  dependencies:
    - core/drupal
    - core/drupalSettings
    - core/drupal.ajax
    - core/once
    # design-tokens (css.base weight) must load before the dashboard component
    # CSS so all --airo-* custom properties are defined when this file runs.
    - ai_content_audit/design-tokens
